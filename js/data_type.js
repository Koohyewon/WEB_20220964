let number = 5;
let str = '문자열 입력';	//""도 묶음 가능
let prime = 1.5123;
let is_ok = true;	//참
let is_not = false;	//거짓
let undefi;	//변수 이름만, 초기화X
let empty = null;	//비어있음	//null로 표기한 것과 초기화를 안한 것은 다르다.

console.log(undefi, empty);	//여러 개 출력